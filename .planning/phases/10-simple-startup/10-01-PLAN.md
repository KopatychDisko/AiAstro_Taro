---
phase: 10-simple-startup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - scripts/aitaro_api.py
  - scripts/aitaro_setup.py
  - src/backend/server/required_env.py
  - tests/test_required_env.py
  - tests/test_aitaro_scripts.py
autonomous: true
requirements:
  - SC-10.1
  - SC-10.2
  - SC-10.3
  - SC-10.6
must_haves:
  truths:
    - "uv run resolves console scripts aitaro-setup and aitaro-api after uv sync"
    - "missing or empty STREAM_API_KEY, OPENAI_API_KEY, or ZEP_API produces a compact missing-key list plus .env.example hint"
    - "optional keys (Langfuse, Qdrant, HuggingFace, Postgres) are never checked by required_env"
    - "pytest pythonpath stays src/backend and repo root (D-07)"
  artifacts:
    - path: "pyproject.toml"
      provides: "[project.scripts], [build-system] setuptools, py-modules for scripts/"
      contains: "aitaro-setup"
    - path: "scripts/aitaro_api.py"
      provides: "aitaro_api.main path-wrapper stub"
      exports: ["main", "ensure_backend_on_path", "find_repo_root"]
    - path: "scripts/aitaro_setup.py"
      provides: "aitaro_setup.main stub"
      exports: ["main"]
    - path: "src/backend/server/required_env.py"
      provides: "shared D-09 env validation"
      exports:
        - "REQUIRED_ENV_KEYS"
        - "missing_required_env_keys"
        - "format_missing_env_message"
        - "require_env_or_exit"
        - "require_env_or_raise"
    - path: "tests/test_required_env.py"
      provides: "Wave 0 env fail-fast tests"
    - path: "tests/test_aitaro_scripts.py"
      provides: "Wave 0 entrypoint module tests"
  key_links:
    - from: "pyproject.toml"
      to: "scripts/aitaro_api.py"
      via: "aitaro-api = aitaro_api:main"
      pattern: "aitaro-api\\s*=\\s*\"aitaro_api:main\""
    - from: "pyproject.toml"
      to: "scripts/aitaro_setup.py"
      via: "aitaro-setup = aitaro_setup:main"
      pattern: "aitaro-setup\\s*=\\s*\"aitaro_setup:main\""
    - from: "tests/test_required_env.py"
      to: "src/backend/server/required_env.py"
      via: "import server.required_env"
      pattern: "required_env|REQUIRED_ENV_KEYS"
---

<objective>
Wave 0 foundation for Phase 10: setuptools `[project.scripts]` packaging (D-01/D-06), path-helper stubs under `scripts/` (D-05), shared `server.required_env` for D-09/D-11/D-12, and Nyquist Wave 0 tests — without wiring lifespan, npm build, or README yet.

Purpose: Prove entrypoints install and env fail-fast contracts before setup/API wiring (hardest constraint first: packaging without shipping `src/`).
Output: Packaged console scripts, `required_env.py`, `tests/test_required_env.py`, `tests/test_aitaro_scripts.py`.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-simple-startup/10-CONTEXT.md
@.planning/phases/10-simple-startup/10-RESEARCH.md
@.planning/phases/10-simple-startup/10-VALIDATION.md

@pyproject.toml
@.env.example
@src/backend/server/app.py
</context>

<interface>
## Contracts this plan creates

- `aitaro_api.find_repo_root() -> Path` — walk cwd/parents for `pyproject.toml` + `src/backend`
- `aitaro_api.ensure_backend_on_path() -> Path` — insert `src/backend` on `sys.path` and prepend `PYTHONPATH` (D-05)
- `aitaro_api.main() -> None` — stub: call `ensure_backend_on_path` only (uvicorn in Plan 03)
- `aitaro_setup.main() -> None` — stub: call `ensure_backend_on_path`, import `require_env_or_exit`, exit on missing keys (full npm/checklist in Plan 02)
- `server.required_env.REQUIRED_ENV_KEYS` — `("STREAM_API_KEY", "OPENAI_API_KEY", "ZEP_API")` per D-09
- `server.required_env.missing_required_env_keys() -> list[str]`
- `server.required_env.format_missing_env_message(missing: list[str]) -> str` — compact list + `.env.example` hint (D-11)
- `server.required_env.require_env_or_exit() -> None` — print to stderr, `SystemExit(1)`
- `server.required_env.require_env_or_raise() -> None` — `RuntimeError` with same message (for lifespan in Plan 03)
</interface>

## Artifacts this phase produces

| Symbol / path | Introduced in |
|---------------|---------------|
| `pyproject.toml` `[project.scripts]` + `[build-system]` | 10-01 |
| `scripts/aitaro_api.py` | 10-01 (full uvicorn in 10-03) |
| `scripts/aitaro_setup.py` | 10-01 (full npm/checklist in 10-02) |
| `src/backend/server/required_env.py` | 10-01 |
| `tests/test_required_env.py` | 10-01 |
| `tests/test_aitaro_scripts.py` | 10-01 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Packaging, path stubs, and required_env helper</name>
  <files>pyproject.toml, scripts/aitaro_api.py, scripts/aitaro_setup.py, src/backend/server/required_env.py</files>
  <read_first>
    pyproject.toml
    .env.example
    .planning/phases/10-simple-startup/10-RESEARCH.md
    .planning/phases/10-simple-startup/10-CONTEXT.md
  </read_first>
  <behavior>
    - missing_required_env_keys returns a key when getenv is None or whitespace-only
    - format_missing_env_message lists each missing key and mentions .env.example
    - require_env_or_exit raises SystemExit(1) when any required key missing; does nothing when all three set
    - REQUIRED_ENV_KEYS is exactly STREAM_API_KEY, OPENAI_API_KEY, ZEP_API (D-09); no Langfuse/Qdrant/HF/Postgres (D-12)
    - After uv sync, `uv run python -c "import aitaro_api, aitaro_setup"` succeeds
  </behavior>
  <action>
    Per D-01 and D-06: add `[project.scripts]` mapping `aitaro-setup = "aitaro_setup:main"` and `aitaro-api = "aitaro_api:main"`. Add `[build-system]` with `requires = ["setuptools>=61"]` and `build-backend = "setuptools.build_meta"`. Add `[tool.setuptools]` with `py-modules = ["aitaro_api", "aitaro_setup"]` and `[tool.setuptools.package-dir]` with `"" = "scripts"`. Do not use `packages = find` under `src/` (D-05 — wrapper only, not editable package layout). Do not change `[tool.pytest.ini_options] pythonpath` (D-07).

    Create `src/backend/server/required_env.py` (Claude discretion / RESEARCH Pattern 3): `REQUIRED_ENV_KEYS` tuple for D-09; `load_dotenv` preferring repo-root `.env` when root is known (or document that callers pass root via load before check); treat None and strip-empty as missing; `format_missing_env_message` compact list + hint to copy `.env.example` → `.env` (D-11); `require_env_or_exit` / `require_env_or_raise`. Never log or print secret values (T-10-01). No default parameter values on new functions — every parameter explicit.

    Create `scripts/aitaro_api.py` with `find_repo_root`, `ensure_backend_on_path` (sys.path insert + PYTHONPATH prepend for reload children per RESEARCH Pattern 2), and `main` that only ensures path (uvicorn deferred to Plan 03). Create `scripts/aitaro_setup.py` with `main` that ensures path then calls `require_env_or_exit` only (npm + UI checklist deferred to Plan 02). Do not run `uv sync` from setup code (D-02). Do not create `aitaro-ui` (D-03).

    Run `uv sync` once after pyproject changes so entry points land in `.venv`.
  </action>
  <acceptance_criteria>
    - pyproject.toml contains aitaro-setup and aitaro-api under [project.scripts] and setuptools py-modules for both
    - [tool.pytest.ini_options] pythonpath still exactly ["src/backend", "."]
    - server.required_env exports REQUIRED_ENV_KEYS with exactly the three D-09 names
    - scripts expose main callables; ensure_backend_on_path makes `from server.required_env import REQUIRED_ENV_KEYS` work without manual shell PYTHONPATH
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv sync && uv run python -c "import aitaro_api, aitaro_setup; from aitaro_api import ensure_backend_on_path; ensure_backend_on_path(); from server.required_env import REQUIRED_ENV_KEYS, require_env_or_raise; assert REQUIRED_ENV_KEYS == ('STREAM_API_KEY', 'OPENAI_API_KEY', 'ZEP_API'); print('ok')"</automated>
  </verify>
  <done>Packaging installs entry modules; required_env implements D-09/D-11/D-12; path stub works; pytest pythonpath untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 tests for required_env and scripts</name>
  <files>tests/test_required_env.py, tests/test_aitaro_scripts.py</files>
  <read_first>
    tests/test_stream_endpoint.py
    tests/test_observability.py
    .planning/phases/10-simple-startup/10-VALIDATION.md
    src/backend/server/required_env.py
    scripts/aitaro_api.py
    scripts/aitaro_setup.py
  </read_first>
  <behavior>
    - test_required_env: all three present → no raise; each key missing alone → listed; empty string → listed; message includes .env.example; message must not contain values of set secrets
    - test_aitaro_scripts: import aitaro_api and aitaro_setup; main is callable; ensure_backend_on_path returns a Path with src/backend; optional later npm tests left for Plan 02
    - Do not use TestClient against real lifespan+MCP
  </behavior>
  <action>
    Add Wave 0 files per 10-VALIDATION.md and RESEARCH Validation Architecture (maps SC-10.1/SC-10.2/SC-10.3). Use monkeypatch for env vars. Import `server.required_env` via existing pytest pythonpath (D-07 — do not edit pytest config). Import script modules as `aitaro_api` / `aitaro_setup` after packaging (or via path that mirrors installed layout). Keep tests fast; no real npm, no real uvicorn, no TestClient against production lifespan.
  </action>
  <acceptance_criteria>
    - tests/test_required_env.py and tests/test_aitaro_scripts.py exist and pass
    - Full `uv run pytest -q` remains green (SC-10.6)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_required_env.py tests/test_aitaro_scripts.py -x && uv run pytest -q</automated>
  </verify>
  <done>Wave 0 Nyquist tests green; suite green; VALIDATION.md Wave 0 gaps for these files closable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → required_env | Untrusted/missing env crosses into fail-fast before MCP/LLM |
| developer machine → console scripts | Local CLI only; no remote input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | server/required_env.py | mitigate | Fail closed on missing/empty keys; format_missing_env_message lists key names only — never log or print secret values |
| T-10-02 | Spoofing | required_env + auth | mitigate | D-09 keys required at setup/lifespan; empty string treated as missing so blank .env cannot bypass |
| T-10-03 | Tampering | npm subprocess | accept | No npm in this plan — mitigated in Plan 02 |
| T-10-04 | Tampering | tarotmcp cwd | accept | No npm cwd in this plan — mitigated in Plan 02 |
| T-10-SC | Tampering | setuptools build-system | mitigate | setuptools only as [build-system] require; RESEARCH Package Legitimacy Audit Approved (PyPA); no new runtime PyPI deps |
</threat_model>

<verification>
- `uv sync` installs aitaro_api / aitaro_setup
- Wave 0 pytest files pass; full suite green
- pytest pythonpath unchanged (D-07)
</verification>

<success_criteria>
- SC-10.3 helper contract live
- SC-10.1/SC-10.2 entrypoint modules importable (full setup/API behavior in later plans)
- SC-10.6 suite still green
</success_criteria>

<output>
Create `.planning/phases/10-simple-startup/10-01-SUMMARY.md` when done
</output>
