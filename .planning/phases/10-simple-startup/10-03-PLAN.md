---
phase: 10-simple-startup
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - scripts/aitaro_api.py
  - src/backend/server/app.py
  - README.md
  - tests/test_readme_quickstart.py
  - tests/test_lifespan_required_env.py
autonomous: true
requirements:
  - SC-10.2
  - SC-10.3
  - SC-10.5
  - SC-10.6
must_haves:
  truths:
    - "uv run aitaro-api starts uvicorn server.app:app with reload True and port 8000 without manual PYTHONPATH"
    - "FastAPI lifespan calls require_env_or_raise before setup_workflow so missing env fails before MCP/LLM init"
    - "README Quick start is four steps (.env, uv sync, aitaro-setup, aitaro-api) plus one UI line; no PYTHONPATH uvicorn"
  artifacts:
    - path: "scripts/aitaro_api.py"
      provides: "uvicorn.run server.app:app host 127.0.0.1 port 8000 reload True"
      exports: ["main"]
    - path: "src/backend/server/app.py"
      provides: "lifespan env fail-fast before setup_workflow"
      contains: "require_env_or_raise"
    - path: "README.md"
      provides: "D-16 four-step Quick start + UI line"
      contains: "uv run aitaro-api"
    - path: "tests/test_readme_quickstart.py"
      provides: "grep-style assertions for D-08/D-16"
    - path: "tests/test_lifespan_required_env.py"
      provides: "lifespan calls require_env before setup_workflow"
  key_links:
    - from: "scripts/aitaro_api.py"
      to: "server.app:app"
      via: "uvicorn.run import string"
      pattern: "server\\.app:app"
    - from: "src/backend/server/app.py"
      to: "src/backend/server/required_env.py"
      via: "require_env_or_raise before setup_workflow"
      pattern: "require_env_or_raise"
    - from: "README.md"
      to: "aitaro-api"
      via: "Quick start step"
      pattern: "uv run aitaro-api"
---

<objective>
Wire daily API launch and docs: `aitaro-api` path wrapper + uvicorn defaults (D-03–D-05, D-04), lifespan `require_env_or_raise` before `setup_workflow` (D-10), README four-step Quick start + UI line without PYTHONPATH uvicorn (D-08, D-16).

Purpose: Complete SC-10.2/SC-10.3/SC-10.5 so developers run `uv run aitaro-api` as the only backend start path.
Output: Complete `aitaro_api.main`, updated `server/app.py` lifespan, rewritten README Quick start, light tests.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-simple-startup/10-CONTEXT.md
@.planning/phases/10-simple-startup/10-RESEARCH.md
@.planning/phases/10-simple-startup/10-VALIDATION.md
@.planning/phases/10-simple-startup/10-01-PLAN.md

@scripts/aitaro_api.py
@src/backend/server/app.py
@src/backend/server/required_env.py
@README.md
@tests/test_stream_endpoint.py
</context>

<interface>
## Contracts this plan completes

- `aitaro_api.main() -> None` — `ensure_backend_on_path()` then `uvicorn.run("server.app:app", host="127.0.0.1", port=8000, reload=True)` (D-04; host discretion from RESEARCH; no argv pass-through)
- `server.app.lifespan` — first statement after entering startup: `require_env_or_raise()`; only then `workflow = await setup_workflow()` (D-10)
- README Quick start steps exactly: (1) `cp .env.example .env` (2) `uv sync` (3) `uv run aitaro-setup` (4) `uv run aitaro-api`; plus one UI line with `login_menu.py` (D-15/D-16); remove all `PYTHONPATH=… uvicorn` snippets (D-08)
</interface>

## Artifacts this phase produces

| Symbol / path | Introduced / completed in |
|---------------|---------------------------|
| Full `aitaro_api.main` + uvicorn | 10-03 |
| lifespan `require_env_or_raise` gate | 10-03 |
| README 4-step Quick start | 10-03 |
| `tests/test_readme_quickstart.py` | 10-03 |
| `tests/test_lifespan_required_env.py` | 10-03 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: aitaro-api uvicorn wrapper and lifespan fail-fast</name>
  <files>scripts/aitaro_api.py, src/backend/server/app.py, tests/test_lifespan_required_env.py</files>
  <read_first>
    scripts/aitaro_api.py
    src/backend/server/app.py
    src/backend/server/required_env.py
    tests/test_stream_endpoint.py
    .planning/phases/10-simple-startup/10-RESEARCH.md
  </read_first>
  <behavior>
    - main sets sys.path and PYTHONPATH then uvicorn.run with import string "server.app:app", reload=True, port=8000, host="127.0.0.1"
    - does not pass the app object when reload=True
    - lifespan invokes require_env_or_raise before any await setup_workflow()
    - when required env missing, setup_workflow is never called (unit test with mocks)
    - stream endpoint tests keep mocked lifespan pattern — do not force real MCP in TestClient tests
    - uvicorn kwargs (host/port/reload/app import string) asserted only in test_lifespan_required_env.py — do not edit tests/test_aitaro_scripts.py (owned by Plan 02)
  </behavior>
  <action>
    Complete `aitaro_api.main` per D-03 (API only), D-04 (fixed reload + port 8000, no argv pass-through), D-05 (wrapper sets path + PYTHONPATH). Use `uvicorn.run("server.app:app", host="127.0.0.1", port=8000, reload=True)` after `ensure_backend_on_path()`. Do not create aitaro-ui.

    In `src/backend/server/app.py` lifespan, call `require_env_or_raise()` from `server.required_env` immediately before `await setup_workflow()` (D-10). Raising aborts startup; do not catch-and-ignore. Keep existing stream route and auth unchanged.

    Add `tests/test_lifespan_required_env.py` only (do not modify `tests/test_aitaro_scripts.py` — Wave 2 parallel ownership with Plan 02): monkeypatch `require_env_or_raise` to raise, patch `setup_workflow` and assert it was not awaited/called; second case require_env passes and setup_workflow called. Also monkeypatch `uvicorn.run` in that same file to assert kwargs (host `127.0.0.1`, port 8000, reload True, app `"server.app:app"`) without binding a port.
  </action>
  <acceptance_criteria>
    - aitaro_api.main uses import-string uvicorn with port 8000 and reload True
    - lifespan orders require_env_or_raise before setup_workflow
    - unit tests in test_lifespan_required_env.py prove ordering and uvicorn kwargs; no secret values in error paths
    - existing stream tests still green with mocked lifespan
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_lifespan_required_env.py tests/test_stream_endpoint.py -x</automated>
  </verify>
  <done>SC-10.2 wrapper and SC-10.3 lifespan fail-fast wired and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: README four-step Quick start + UI line</name>
  <files>README.md, tests/test_readme_quickstart.py</files>
  <read_first>
    README.md
    .planning/phases/10-simple-startup/10-CONTEXT.md
  </read_first>
  <behavior>
    - README contains uv run aitaro-setup and uv run aitaro-api
    - README does not contain PYTHONPATH=...uvicorn patterns
    - Quick start documents .env copy, uv sync, aitaro-setup, aitaro-api, plus login_menu.py UI line
  </behavior>
  <action>
    Rewrite README Quick start to exactly four steps per D-16: (1) copy `.env.example` → `.env` and fill required keys including STREAM_API_KEY, OPENAI_API_KEY, ZEP_API; (2) `uv sync`; (3) `uv run aitaro-setup`; (4) `uv run aitaro-api`. Add one UI line using `login_menu.py` from `src/frontend` (D-15). Remove every raw `PYTHONPATH=src/backend uv run uvicorn …` and venv uvicorn PYTHONPATH variant (D-08). Keep architecture/API docs elsewhere in README intact unless they contradict the new start path — if they still say PYTHONPATH uvicorn, update those mentions too. Note host/port `127.0.0.1:8000` if documenting the URL.

    Add `tests/test_readme_quickstart.py` that reads README.md text and asserts presence of `uv run aitaro-api` and `uv run aitaro-setup`, asserts absence of `PYTHONPATH=` combined with `uvicorn` (SC-10.5 / VALIDATION grep gate).
  </action>
  <acceptance_criteria>
    - Quick start ≤5 steps: four backend steps + one UI line
    - no PYTHONPATH uvicorn in README
    - test_readme_quickstart.py passes; full pytest -q green (SC-10.6); pytest pythonpath still unchanged (D-07)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_readme_quickstart.py -x && rg -n "PYTHONPATH=.*uvicorn|uv run aitaro-api|uv run aitaro-setup" README.md && uv run pytest -q</automated>
  </verify>
  <done>SC-10.5 and SC-10.6 met; D-08/D-16 locked in README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → FastAPI lifespan | Missing secrets must abort before workflow/MCP |
| developer → aitaro-api CLI | Local bind 127.0.0.1 only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | lifespan + required_env | mitigate | require_env_or_raise before setup_workflow; RuntimeError message lists key names only — never log getenv values |
| T-10-02 | Spoofing | STREAM_API_KEY at boot | mitigate | Empty/missing key fails closed at lifespan; auth remains secondary per-request guard |
| T-10-05 | Information Disclosure | uvicorn bind host | mitigate | Default host 127.0.0.1 (not 0.0.0.0) for local API |
| T-10-03 | Tampering | npm subprocess | accept | No npm in this plan (covered in 10-02) |
| T-10-04 | Tampering | tarotmcp cwd | accept | No npm cwd in this plan (covered in 10-02) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Lifespan ordering unit tests pass
- README grep/test gates pass
- Full pytest -q green; D-07 pythonpath unchanged
</verification>

<success_criteria>
- SC-10.2: backend via uv run aitaro-api without manual PYTHONPATH
- SC-10.3: env fail-fast before MCP/LLM init
- SC-10.5: README ≤5 steps matching scripts
- SC-10.6: pytest -q green
</success_criteria>

<output>
Create `.planning/phases/10-simple-startup/10-03-SUMMARY.md` when done
</output>
