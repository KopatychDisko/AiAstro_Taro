---
phase: 10-simple-startup
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - scripts/aitaro_setup.py
  - src/backend/agents/taro/factory.py
  - tests/test_aitaro_scripts.py
  - tests/test_tarot_factory_message.py
autonomous: true
requirements:
  - SC-10.1
  - SC-10.4
must_haves:
  truths:
    - "aitaro-setup runs env check then npm install/build in src/tarotmcp without calling uv sync"
    - "setup prints Streamlit login_menu.py command and Postgres reminder without validating POSTGRESQL_*"
    - "missing tarot MCP dist FileNotFoundError mentions uv run aitaro-setup"
  artifacts:
    - path: "scripts/aitaro_setup.py"
      provides: "full setup: require_env_or_exit, build_tarot_mcp, checklist stdout"
      exports: ["main", "build_tarot_mcp"]
    - path: "src/backend/agents/taro/factory.py"
      provides: "FileNotFoundError aligned with aitaro-setup"
      contains: "aitaro-setup"
    - path: "tests/test_tarot_factory_message.py"
      provides: "SC-10.4 factory message assertion"
  key_links:
    - from: "scripts/aitaro_setup.py"
      to: "src/backend/server/required_env.py"
      via: "require_env_or_exit after path ensure"
      pattern: "require_env_or_exit"
    - from: "scripts/aitaro_setup.py"
      to: "src/tarotmcp"
      via: "subprocess cwd=repo_root/src/tarotmcp"
      pattern: "tarotmcp"
    - from: "src/backend/agents/taro/factory.py"
      to: "aitaro-setup"
      via: "FileNotFoundError message"
      pattern: "aitaro-setup"
---

<objective>
Implement `aitaro-setup` end-to-end (D-02, D-10–D-15): env fail-fast first, fixed-argv npm build under `src/tarotmcp`, stdout checklist with Streamlit `login_menu.py` + Postgres reminder; align tarot factory missing-dist error with `uv run aitaro-setup` (SC-10.4).

Purpose: One documented setup path that builds MCP and validates required env without owning `uv sync` or starting the UI.
Output: Complete `scripts/aitaro_setup.py`, updated factory message, tests covering npm monkeypatch + factory text.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-simple-startup/10-CONTEXT.md
@.planning/phases/10-simple-startup/10-RESEARCH.md
@.planning/phases/10-simple-startup/10-VALIDATION.md
@.planning/phases/10-simple-startup/10-01-PLAN.md

@scripts/aitaro_setup.py
@src/backend/server/required_env.py
@src/backend/agents/taro/factory.py
@src/tarotmcp/package.json
@src/frontend/login_menu.py
</context>

<interface>
## Contracts this plan extends

- `aitaro_setup.build_tarot_mcp(repo_root: Path) -> None` — `subprocess.run(["npm", "install"], cwd=tarotmcp, check=True)` then `["npm", "run", "build"]`; `tarotmcp = repo_root / "src" / "tarotmcp"` only (T-10-04)
- `aitaro_setup.main() -> None` — order: `ensure_backend_on_path` → `require_env_or_exit` (D-10) → `build_tarot_mcp` → print checklist (D-13–D-15)
- Checklist stdout must include: MCP OK line, required-env OK naming the three keys, `uv run aitaro-api` next step, `cd src/frontend && … streamlit run login_menu.py`, and Postgres reminder without validating `POSTGRESQL_*` (D-14)
</interface>

## Artifacts this phase produces

| Symbol / path | Introduced / completed in |
|---------------|---------------------------|
| `aitaro_setup.build_tarot_mcp` | 10-02 |
| Full `aitaro_setup.main` checklist | 10-02 |
| `agents.taro.factory` FileNotFoundError text with `aitaro-setup` | 10-02 |
| `tests/test_tarot_factory_message.py` | 10-02 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete aitaro-setup (env, npm, checklist)</name>
  <files>scripts/aitaro_setup.py, tests/test_aitaro_scripts.py</files>
  <read_first>
    scripts/aitaro_setup.py
    scripts/aitaro_api.py
    src/backend/server/required_env.py
    src/tarotmcp/package.json
    .planning/phases/10-simple-startup/10-RESEARCH.md
  </read_first>
  <behavior>
    - main calls require_env_or_exit before any npm subprocess (RESEARCH recommendation / faster fail)
    - build_tarot_mcp uses fixed argv lists only — never shell=True, never user-controlled command strings (T-10-03)
    - cwd is always repo_root / "src" / "tarotmcp"; missing directory raises FileNotFoundError
    - main does not invoke uv sync (D-02)
    - stdout after success includes login_menu.py and Postgres reminder text (D-13, D-14, D-15)
    - monkeypatched subprocess tests prove argv and cwd without running real npm in CI unit path
  </behavior>
  <action>
    Implement full `aitaro_setup` per D-02 and D-10–D-15. Reuse `find_repo_root` / `ensure_backend_on_path` from `aitaro_api` (import shared helpers — prefer importing from `aitaro_api` to avoid duplicating path logic; if import-of-sibling is awkward under scripts packaging, extract a tiny shared helper module under `scripts/` only if needed — do not move helpers into `src/backend` packaging). After path ensure, call `require_env_or_exit` from `server.required_env` (D-09/D-10/D-11). Then `build_tarot_mcp(repo_root)` with `subprocess.run` fixed argv `["npm", "install"]` and `["npm", "run", "build"]`, `check=True`, `cwd` resolved under repo root only (T-10-04). Print checklist: Tarot MCP OK; Required env OK listing STREAM_API_KEY, OPENAI_API_KEY, ZEP_API; Next: `uv run aitaro-api`; UI optional with `cd src/frontend && uv run streamlit run login_menu.py` (D-15) and text that UI needs POSTGRESQL_* without validating them (D-14). Do not check Langfuse/Qdrant/HF (D-12). Do not start Streamlit (D-13). Do not add aitaro-ui (D-03).

    Extend `tests/test_aitaro_scripts.py` with monkeypatch of `subprocess.run` asserting call count, argv, and cwd ending in `src/tarotmcp`.
  </action>
  <acceptance_criteria>
    - aitaro_setup.main performs env check before npm
    - no uv sync, no shell=True, no optional-key warnings
    - checklist mentions login_menu.py and Postgres reminder
    - unit tests pass with mocked subprocess
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_aitaro_scripts.py tests/test_required_env.py -x</automated>
  </verify>
  <done>SC-10.1 setup path implemented and unit-tested; D-02/D-10/D-13/D-14/D-15 honored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align tarot factory missing-dist message</name>
  <files>src/backend/agents/taro/factory.py, tests/test_tarot_factory_message.py</files>
  <read_first>
    src/backend/agents/taro/factory.py
  </read_first>
  <behavior>
    - When dist/index.js missing, FileNotFoundError message contains "uv run aitaro-setup"
    - May still mention legacy cd src/tarotmcp npm one-liner as secondary hint
  </behavior>
  <action>
    Update the FileNotFoundError string in `create_tarot_agent` (Claude discretion / RESEARCH Pitfall 6) so primary remediation is `uv run aitaro-setup`, optionally keeping `(or: cd src/tarotmcp && npm install && npm run build)`. Do not change MCP path depth or MultiServerMCPClient config. Add `tests/test_tarot_factory_message.py` that triggers the missing-file branch (monkeypatch `os.path.isfile` to False or point path check) and asserts `"aitaro-setup"` in the exception message (SC-10.4 / VALIDATION map).
  </action>
  <acceptance_criteria>
    - factory error text includes aitaro-setup
    - test_tarot_factory_message.py passes
    - full suite still green
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_tarot_factory_message.py tests/test_aitaro_scripts.py -x && uv run pytest -q</automated>
  </verify>
  <done>SC-10.4 satisfied; factory and setup docs agree on aitaro-setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup CLI → npm subprocess | Local build orchestration; argv must stay fixed |
| setup CLI → filesystem cwd | Must stay under repo_root/src/tarotmcp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | aitaro_setup + required_env | mitigate | require_env_or_exit before npm; print key names only, never secret values |
| T-10-03 | Tampering | aitaro_setup.build_tarot_mcp | mitigate | Fixed argv lists only: ["npm","install"] and ["npm","run","build"]; shell=True forbidden; no user argv pass-through |
| T-10-04 | Tampering | tarotmcp cwd | mitigate | Resolve cwd exclusively as repo_root / "src" / "tarotmcp"; raise if directory missing; never accept caller-supplied cwd path from CLI args |
| T-10-SC | Tampering | npm/pip installs | accept | No new PyPI packages this plan; npm uses project-local package.json only |
</threat_model>

<verification>
- Mocked npm path tests pass
- Factory message test passes
- pytest -q green
</verification>

<success_criteria>
- SC-10.1: setup builds MCP path + checklist
- SC-10.4: factory points at aitaro-setup
</success_criteria>

<output>
Create `.planning/phases/10-simple-startup/10-02-SUMMARY.md` when done
</output>
