---
phase: 09-backend-structure-refactor
plan: 06
type: execute
wave: 6
depends_on:
  - "09-05"
files_modified:
  - tests/conftest.py
  - tests/test_stream_endpoint.py
  - tests/test_observability.py
  - tests/test_routing.py
  - tests/test_taro_cards.py
  - tests/test_memory_tools.py
  - tests/test_router.py
  - tests/test_add_memory_resilience.py
  - src/backend/app.py
  - src/backend/auth.py
  - src/backend/observability.py
  - src/backend/schemas.py
  - src/backend/models.py
  - src/backend/graph/
autonomous: false
requirements:
  - SC-04
  - SC-01
  - SC-02
must_haves:
  truths:
    - "pytest -q is green against server.* and agents.* only"
    - "No leftover graph/ package or flat root app/auth/models/schemas/observability modules"
    - "No deprecation re-export shims"
    - "Stream auth tests still pass (no auth bypass)"
  artifacts:
    - path: "tests/test_router.py"
      provides: "Patches agents.workflow.create_agents and AsyncZep"
      contains: "agents.workflow.create_agents"
    - path: "tests/test_stream_endpoint.py"
      provides: "Auth + stream schema tests against server.*"
      contains: "server.auth"
  key_links:
    - from: "tests/test_router.py"
      to: "agents.workflow"
      via: "unittest.mock.patch at import site"
      pattern: "agents\\.workflow\\.create_agents"
    - from: "tests/test_memory_tools.py"
      to: "agents.memory.tools"
      via: "patch zep.graph.search"
      pattern: "agents\\.memory\\.tools"
---

<objective>
Hard-cut to `server.*` / `agents.*`: rewrite all test imports and patch targets, delete legacy flat modules and `graph/`, clear stale caches, and prove SC-04 with full pytest green plus grep gates — locked D-04 (completing D-01–D-03 cleanup).

Purpose: Single import vocabulary; behavior unchanged; stream auth coverage remains green.
Output: Updated tests/**, deleted old backend modules, green suite, empty legacy-import grep.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-structure-refactor/09-CONTEXT.md
@.planning/phases/09-backend-structure-refactor/09-RESEARCH.md
@.planning/phases/09-backend-structure-refactor/09-VALIDATION.md
@.planning/phases/09-backend-structure-refactor/09-04-SUMMARY.md
@.planning/phases/09-backend-structure-refactor/09-05-SUMMARY.md

@tests/conftest.py
@tests/test_stream_endpoint.py
@tests/test_observability.py
@tests/test_routing.py
@tests/test_taro_cards.py
@tests/test_memory_tools.py
@tests/test_router.py
@tests/test_add_memory_resilience.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite all test imports and patch targets</name>
  <files>tests/conftest.py, tests/test_stream_endpoint.py, tests/test_observability.py, tests/test_routing.py, tests/test_taro_cards.py, tests/test_memory_tools.py, tests/test_router.py, tests/test_add_memory_resilience.py</files>
  <read_first>
    tests/conftest.py
    tests/test_stream_endpoint.py
    tests/test_observability.py
    tests/test_routing.py
    tests/test_taro_cards.py
    tests/test_memory_tools.py
    tests/test_router.py
    tests/test_add_memory_resilience.py
    .planning/phases/09-backend-structure-refactor/09-RESEARCH.md
  </read_first>
  <behavior>
    - test_stream_endpoint: server.auth / server.schemas; missing/invalid key 401; valid key streams ExtractData
    - test_observability: server.observability; no Langfuse keys → empty callbacks
    - test_routing: agents.routing capped_tools_condition
    - test_taro_cards: agents.models.TaroCard and agents.cards.mapping
    - test_memory_tools: agents.memory.tools; patch agents.memory.tools.zep.graph.search
    - test_router / test_add_memory_resilience: patch agents.workflow.create_agents and agents.workflow.AsyncZep; import setup_workflow from agents.workflow inside patch context
    - conftest: Agents/RouterOutput/Summarize from agents.state
  </behavior>
  <action>
    Rewrite every test import/patch per RESEARCH Test import + patch map (D-04 hard cut — no temporary dual imports).

    Exact targets: agents.state in conftest; server.observability / server.auth / server.schemas / server.app as applicable; agents.routing; agents.models + agents.cards.mapping; agents.memory.tools with patch agents.memory.tools.zep.graph.search; test_router and test_add_memory_resilience patch agents.workflow.create_agents and agents.workflow.AsyncZep and import setup_workflow inside the patch context.

    Do not patch agents.factories.create_agents if workflow does from .factories import create_agents — patch lookup site agents.workflow.create_agents only. Leave pythonpath ["src/backend"]. Do not delete old production modules in this task (Task 2).
  </action>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && python -m pytest tests/test_stream_endpoint.py tests/test_observability.py tests/test_routing.py tests/test_taro_cards.py tests/test_memory_tools.py tests/test_router.py tests/test_add_memory_resilience.py -q</automated>
  </verify>
  <done>All eight test modules import server.* / agents.* only and pass against the new tree (legacy modules may still exist on disk until Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy modules, clear caches, full suite + grep gates</name>
  <files>src/backend/app.py, src/backend/auth.py, src/backend/observability.py, src/backend/schemas.py, src/backend/models.py, src/backend/graph/</files>
  <read_first>
    .planning/phases/09-backend-structure-refactor/09-RESEARCH.md
  </read_first>
  <action>
    Complete D-04 hard cut: delete src/backend/app.py, auth.py, observability.py, schemas.py, models.py, and entire src/backend/graph/. No re-export shims (D-03/D-04). Clear __pycache__ under src/backend. Confirm pyproject.toml pythonpath remains ["src/backend"]. Run python -m pytest -q (SC-04). Grep gate must be empty: from graph|import graph|from auth import|from schemas import|from models import|from observability import|uvicorn app:app across src/backend tests README.md. Confirm graph/ and root app.py gone. Security: test_stream_endpoint must pass — treat auth failures as high severity and fix before finishing.
  </action>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && python -m pytest -q && test ! -e src/backend/graph && test ! -e src/backend/app.py && ! rg -n "from graph\\.|import graph|from auth import|from schemas import|from models import|from observability import|uvicorn app:app" src/backend tests README.md</automated>
  </verify>
  <done>Legacy tree gone; pytest -q green; grep gates empty; stream auth tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm live entrypoint docs match runnable command</name>
  <files>README.md, src/backend/server/app.py</files>
  <what-built>
    Hard-cut backend layout with server/ + agents/, tests green, README documenting uvicorn server.app:app.
  </what-built>
  <action>
    Pause for human verification of D-03 entrypoint and D-04 hard-cut cleanliness after automated suite/grep gates pass. Fix only if human reports issues; then re-run pytest + grep gates.
  </action>
  <how-to-verify>
    1. Confirm README shows cd src/backend then uv run uvicorn server.app:app --reload --port 8000.
    2. Optional live smoke: POST /stream without key → 401; with STREAM_API_KEY → stream.
    3. Confirm src/backend/graph absent and no root app.py shim.
  </how-to-verify>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && rg -n "uvicorn server.app:app" README.md && test ! -e src/backend/graph && test ! -e src/backend/app.py</automated>
  </verify>
  <done>Human typed approved; README entrypoint correct; no legacy graph/app.py shim.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → server.auth | Tests exercise real auth dependency after move |
| deleted legacy modules | Must not leave unprotected alternate entrypoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing / Elevation | /stream auth after cut | mitigate | test_stream_endpoint green; no shim bypassing Depends(verify_stream_api_key) |
| T-09-02 | Tampering | leftover graph/ or app.py | mitigate | Delete old modules; grep gate forbids graph/flat imports and uvicorn app:app |
| T-09-03 | Denial of Service | wrong mock patches | mitigate | Patch agents.workflow / agents.memory.tools lookup sites only |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
- python -m pytest -q green
- No graph/ or root app.py
- Grep gate empty
- README has uvicorn server.app:app
</verification>

<success_criteria>
- SC-04 pytest green; D-04 hard cut complete; auth not weakened
</success_criteria>

<output>
Create `.planning/phases/09-backend-structure-refactor/09-06-SUMMARY.md` when done
</output>
