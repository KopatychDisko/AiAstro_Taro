---
phase: 09-backend-structure-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - tests/conftest.py
  - tests/test_stream_endpoint.py
  - tests/test_observability.py
  - tests/test_routing.py
  - tests/test_taro_cards.py
  - tests/test_memory_tools.py
  - tests/test_router.py
  - tests/test_add_memory_resilience.py
  - src/backend/app.py
  - src/backend/auth.py
  - src/backend/observability.py
  - src/backend/schemas.py
  - src/backend/models.py
  - src/backend/graph/
autonomous: false
requirements:
  - SC-04
  - SC-01
  - SC-02
must_haves:
  truths:
    - "pytest -q is green against server.* and agents.* only"
    - "No leftover graph/ package or flat root app/auth/models/schemas/observability modules"
    - "No deprecation re-export shims"
    - "Stream auth tests still pass (no auth bypass)"
  artifacts:
    - path: "tests/test_router.py"
      provides: "Patches agents.workflow.create_agents and AsyncZep"
      contains: "agents.workflow.create_agents"
    - path: "tests/test_stream_endpoint.py"
      provides: "Auth + stream schema tests against server.*"
      contains: "server.auth"
  key_links:
    - from: "tests/test_router.py"
      to: "agents.workflow"
      via: "unittest.mock.patch at import site"
      pattern: "agents\\.workflow\\.create_agents"
    - from: "tests/test_memory_tools.py"
      to: "agents.memory.tools"
      via: "patch zep.graph.search"
      pattern: "agents\\.memory\\.tools"
---

<objective>
Hard-cut to `server.*` / `agents.*`: rewrite all test imports and patch targets, delete legacy flat modules and `graph/`, clear stale caches, and prove SC-04 with full pytest green plus grep gates — implementing locked D-04 (and completing D-01–D-03 cleanup).

Purpose: Single import vocabulary with no dual trees or shims; behavior unchanged; stream auth coverage remains green.
Output: Updated `tests/**`, deleted old backend modules, green suite, empty legacy-import grep.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-structure-refactor/09-CONTEXT.md
@.planning/phases/09-backend-structure-refactor/09-RESEARCH.md
@.planning/phases/09-backend-structure-refactor/09-VALIDATION.md
@.planning/phases/09-backend-structure-refactor/09-01-SUMMARY.md
@.planning/phases/09-backend-structure-refactor/09-02-SUMMARY.md

@tests/conftest.py
@tests/test_stream_endpoint.py
@tests/test_observability.py
@tests/test_routing.py
@tests/test_taro_cards.py
@tests/test_memory_tools.py
@tests/test_router.py
@tests/test_add_memory_resilience.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite all test imports and patch targets</name>
  <files>tests/conftest.py, tests/test_stream_endpoint.py, tests/test_observability.py, tests/test_routing.py, tests/test_taro_cards.py, tests/test_memory_tools.py, tests/test_router.py, tests/test_add_memory_resilience.py</files>
  <read_first>
    tests/conftest.py
    tests/test_stream_endpoint.py
    tests/test_observability.py
    tests/test_routing.py
    tests/test_taro_cards.py
    tests/test_memory_tools.py
    tests/test_router.py
    tests/test_add_memory_resilience.py
    .planning/phases/09-backend-structure-refactor/09-RESEARCH.md
  </read_first>
  <behavior>
    - test_stream_endpoint: imports server.auth / server.schemas; missing/invalid key still 401; valid key streams ExtractData shape
    - test_observability: imports server.observability; no Langfuse keys → empty callbacks
    - test_routing: imports agents.routing capped_tools_condition
    - test_taro_cards: imports agents.models.TaroCard and agents.cards.mapping helpers
    - test_memory_tools: imports agents.memory.tools; patches agents.memory.tools.zep.graph.search
    - test_router / test_add_memory_resilience: patch agents.workflow.create_agents and agents.workflow.AsyncZep; import setup_workflow from agents.workflow inside patch context
    - conftest: Agents/RouterOutput/Summarize from agents.state
  </behavior>
  <action>
    Rewrite every test import/patch per RESEARCH "Test import + patch map" (D-04 hard cut — no temporary dual imports).

    Exact target map:
    - conftest: `from agents.state import Agents, RouterOutput, Summarize`
    - test_observability: `from server.observability import ...`
    - test_stream_endpoint: `from server.auth import ...`, `from server.schemas import ...` (and app under test must use `server.app` if previously imported root app)
    - test_routing: `from agents.routing import ...`
    - test_taro_cards: `from agents.models import TaroCard`, `from agents.cards.mapping import ...`
    - test_memory_tools: `from agents.memory.tools import search_facts, search_nodes`; `patch("agents.memory.tools.zep.graph.search", ...)`
    - test_router + test_add_memory_resilience: `patch("agents.workflow.create_agents", ...)`, `patch("agents.workflow.AsyncZep", ...)`; `from agents.workflow import setup_workflow` inside the patch context (keep existing import-inside-patch pattern)

    Do not patch definition modules if workflow does `from .factories import create_agents` — patch lookup site `agents.workflow.create_agents` only. Leave `pyproject.toml` pythonpath as `["src/backend"]`. Do not delete old production modules in this task yet (Task 2) — but tests must no longer reference them.
  </action>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && python -m pytest tests/test_stream_endpoint.py tests/test_observability.py tests/test_routing.py tests/test_taro_cards.py tests/test_memory_tools.py -q</automated>
  </verify>
  <done>All listed tests import server.* / agents.* only; stream auth and agent unit tests pass (router tests may still need old graph deleted or both trees — if they fail because dual imports, proceed to Task 2 immediately in same session after this rewrite).</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy modules, clear caches, full suite + grep gates</name>
  <files>src/backend/app.py, src/backend/auth.py, src/backend/observability.py, src/backend/schemas.py, src/backend/models.py, src/backend/graph/</files>
  <read_first>
    .planning/phases/09-backend-structure-refactor/09-RESEARCH.md
  </read_first>
  <action>
    Complete D-04 hard cut:

    1. Delete legacy production modules: `src/backend/app.py`, `auth.py`, `observability.py`, `schemas.py`, `models.py`, and the entire `src/backend/graph/` tree. Do not leave re-export shims at those paths (D-03/D-04).
    2. Remove stale `__pycache__` / `.pyc` under `src/backend` (find+delete) so old modules cannot shadow imports.
    3. Confirm `pyproject.toml` still has `pythonpath = ["src/backend"]` only — no new packaging.
    4. Run full suite `python -m pytest -q` — must be green (SC-04).
    5. Run post-cut grep gates (must be empty matches):
       - `rg -n "from graph|import graph|from auth import|from schemas import|from models import|from observability import|uvicorn app:app" src/backend tests README.md`
       - Confirm `src/backend/graph` directory does not exist
       - Confirm no root `src/backend/app.py`
    6. Security gate: `tests/test_stream_endpoint.py` must pass — auth not weakened. If auth tests fail, treat as high severity and fix before finishing (do not ship bypass).

    Atomic commit message style when committing this task: hard-cut delete + test green.
  </action>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && python -m pytest -q && test ! -e src/backend/graph && test ! -e src/backend/app.py && ! rg -n "from graph\\.|import graph|from auth import|from schemas import|from models import|from observability import|uvicorn app:app" src/backend tests README.md</automated>
  </verify>
  <done>Legacy tree gone; pytest -q green; grep gates empty; stream auth tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm live entrypoint docs match runnable command</name>
  <files>README.md, src/backend/server/app.py</files>
  <what-built>
    Hard-cut backend layout with `server/` + `agents/`, tests green, README documenting `uvicorn server.app:app`. Auth and stream unit tests automated-green.
  </what-built>
  <action>
    Pause for human verification of D-03 entrypoint and D-04 hard-cut cleanliness after automated suite/grep gates pass. Do not change code unless the human reports issues; then fix and re-run pytest + grep gates.
  </action>
  <how-to-verify>
    1. From repo root, confirm README run section shows: `cd src/backend` then `uv run uvicorn server.app:app --reload --port 8000`.
    2. Optional live smoke (if env + MCP built): start uvicorn with that command; POST /stream without key → 401; with valid STREAM_API_KEY → stream (auth must not be open).
    3. Confirm `src/backend/graph` is absent and there is no root `app.py` shim.
  </how-to-verify>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && rg -n "uvicorn server.app:app" README.md && test ! -e src/backend/graph && test ! -e src/backend/app.py</automated>
  </verify>
  <done>Human typed approved; README entrypoint correct; no legacy graph/app.py shim.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → server.auth | Tests must exercise real auth dependency paths after move |
| deleted legacy modules | Removal must not leave unprotected alternate entrypoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing / Elevation | /stream auth after cut | mitigate | test_stream_endpoint must stay green; no shim bypassing Depends(verify_stream_api_key) |
| T-09-02 | Tampering | leftover graph/ or app.py | mitigate | Delete old modules; grep gate forbids graph/flat imports and uvicorn app:app |
| T-09-03 | Denial of Service | wrong mock patches hitting live Zep/MCP | mitigate | Patch at agents.workflow / agents.memory.tools lookup sites only |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
- python -m pytest -q green
- No src/backend/graph or root app.py
- Grep gate empty for legacy imports and uvicorn app:app
- README has uvicorn server.app:app
</verification>

<success_criteria>
- SC-04: public imports + pytest -q green; behavior unchanged
- D-04: old modules deleted; no re-export layer
- D-01–D-03 fully enforced in tree and docs
- Stream auth not weakened (automated + human checkpoint)
</success_criteria>

<output>
Create `.planning/phases/09-backend-structure-refactor/09-03-SUMMARY.md` when done
</output>
