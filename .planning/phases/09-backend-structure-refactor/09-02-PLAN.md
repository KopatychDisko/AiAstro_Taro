---
phase: 09-backend-structure-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/backend/server/__init__.py
  - src/backend/server/app.py
  - src/backend/server/auth.py
  - src/backend/server/observability.py
  - src/backend/server/schemas.py
  - README.md
autonomous: true
requirements:
  - SC-01
must_haves:
  truths:
    - "HTTP concerns live under server/ (app, auth, schemas, observability)"
    - "server imports agents one-way; agents never imports server"
    - "README documents uvicorn server.app:app from src/backend"
    - "Stream auth logic is byte-identical in behavior (compare_digest, fail-closed)"
  artifacts:
    - path: "src/backend/server/app.py"
      provides: "FastAPI app + /stream lifespan"
      exports: ["app"]
    - path: "src/backend/server/auth.py"
      provides: "verify_stream_api_key"
      contains: "secrets.compare_digest"
    - path: "src/backend/server/schemas.py"
      provides: "ExtractData, UserData"
      contains: "from agents.models import TaroCard"
    - path: "README.md"
      provides: "Entrypoint docs"
      contains: "uvicorn server.app:app"
  key_links:
    - from: "src/backend/server/app.py"
      to: "agents.setup_workflow"
      via: "from agents import setup_workflow"
      pattern: "from agents import setup_workflow"
    - from: "src/backend/server/app.py"
      to: "src/backend/server/auth.py"
      via: "Depends(verify_stream_api_key)"
      pattern: "verify_stream_api_key"
    - from: "src/backend/server/schemas.py"
      to: "src/backend/agents/models.py"
      via: "TaroCard import"
      pattern: "from agents\\.models import TaroCard"
---

<objective>
Create the `server/` HTTP package wired to `agents.setup_workflow`, place ExtractData/UserData in server.schemas importing TaroCard from agents.models, and update README to `uvicorn server.app:app` — implementing SC-01 and locked D-01, D-02, D-03.

Purpose: Complete the two-package split so the public ASGI entrypoint is explicit and auth stays on `/stream`.
Output: `src/backend/server/**` + README entrypoint/layout updates; legacy root modules still present until Plan 03.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-structure-refactor/09-CONTEXT.md
@.planning/phases/09-backend-structure-refactor/09-RESEARCH.md
@.planning/phases/09-backend-structure-refactor/09-VALIDATION.md
@.planning/phases/09-backend-structure-refactor/09-01-SUMMARY.md

@src/backend/app.py
@src/backend/auth.py
@src/backend/observability.py
@src/backend/schemas.py
@README.md
@pyproject.toml
</context>

<interface>
## Contracts consumed from Plan 01

- `from agents import setup_workflow`
- `from agents.models import TaroCard`

## Contracts this plan creates

- `server.app:app` — ASGI application object named `app`
- `server.auth.verify_stream_api_key` — FastAPI dependency (unchanged security semantics)
- `server.observability.build_langfuse_callbacks`
- `server.schemas.ExtractData`, `server.schemas.UserData`
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Create server package with one-way agents imports</name>
  <files>src/backend/server/__init__.py, src/backend/server/app.py, src/backend/server/auth.py, src/backend/server/observability.py, src/backend/server/schemas.py</files>
  <read_first>
    src/backend/app.py
    src/backend/auth.py
    src/backend/observability.py
    src/backend/schemas.py
    src/backend/agents/__init__.py
    .planning/phases/09-backend-structure-refactor/09-RESEARCH.md
  </read_first>
  <action>
    Create `src/backend/server/` per D-01 and D-02 (server = HTTP only).

    1. Copy `auth.py` → `server/auth.py` with zero logic changes: keep `secrets.compare_digest`, fail-closed 500 when STREAM_API_KEY unset, X-API-Key + Bearer acceptance, 401 on missing/invalid. Do not weaken stream auth (T-09-01 / ASVS L1 access control).
    2. Copy `observability.py` → `server/observability.py` unchanged in behavior.
    3. Create `server/schemas.py` with `ExtractData` and `UserData` from old schemas; import `TaroCard` via `from agents.models import TaroCard` (discretion: no duplicate TaroCard in server; no ExtractData in agents).
    4. Create `server/app.py` from old `app.py` with imports rewritten: `from server.auth import verify_stream_api_key`, `from server.observability import build_langfuse_callbacks`, `from server.schemas import ExtractData, UserData`, `from agents import setup_workflow`. Keep FastAPI object named `app`, same `/stream` route and lifespan behavior.
    5. Add empty or minimal `server/__init__.py`.

    Hard rules: agents must not import server (grep after write). No root `app.py` shim that re-exports server (D-03/D-04 — leave old root files for Plan 03 delete, but do not add new shims). Do not change `pyproject.toml` pythonpath (stays `["src/backend"]`).
  </action>
  <verify>
    <automated>cd /home/egor/projects/AiTaro &amp;&amp; PYTHONPATH=src/backend python -c "from server.app import app; from server.auth import verify_stream_api_key; from server.schemas import ExtractData, UserData; from agents.models import TaroCard; assert hasattr(app, 'routes'); print('ok')" &amp;&amp; ! rg -n "from server|import server" src/backend/agents &amp;&amp; rg -n "compare_digest" src/backend/server/auth.py</automated>
  </verify>
  <done>server.app imports; auth retains compare_digest; agents has zero imports of server.</done>
</task>

<task type="auto">
  <name>Task 2: Update README entrypoint and layout docs</name>
  <files>README.md</files>
  <read_first>
    README.md
  </read_first>
  <action>
    Update README per D-03: replace `uvicorn app:app` with `uvicorn server.app:app` (still `cd src/backend` then `uv run uvicorn server.app:app --reload --port 8000`). Update any backend layout section that describes flat `app.py` / nested `graph/` to describe `server/` and `agents/` per RESEARCH tree (brief — do not invent new product docs). Do not add a root compatibility shim instruction. Leave pytest pythonpath note as `src/backend`.
  </action>
  <verify>
    <automated>cd /home/egor/projects/AiTaro &amp;&amp; rg -n "uvicorn server.app:app" README.md &amp;&amp; ! rg -n "uvicorn app:app" README.md</automated>
  </verify>
  <done>README documents server.app:app only; no legacy uvicorn app:app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server.app /stream | Untrusted HTTP; API key required |
| server → agents | Trusted internal import of orchestration types/workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | server.auth.verify_stream_api_key | mitigate | Relocate auth without changing compare_digest / fail-closed / header+Bearer behavior; block if any weakening |
| T-09-02 | Elevation of Privilege | Compatibility shims | mitigate | No root app.py re-export; D-03/D-04 — dual tree only until Plan 03 delete |
| T-09-03 | Information Disclosure | server.observability | accept | Langfuse keys remain env-only; move file only |
| T-09-04 | Tampering | server.schemas | mitigate | Keep Pydantic validation on UserData/ExtractData; TaroCard from agents.models not duplicated loosely |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
- `from server.app import app` works with PYTHONPATH=src/backend
- agents does not import server
- README has `uvicorn server.app:app` and lacks `uvicorn app:app`
- Auth file still uses secrets.compare_digest
</verification>

<success_criteria>
- SC-01: clear server package with app, auth, schemas, observability
- D-01/D-02 server half complete with one-way imports
- D-03 README entrypoint updated (hard cut of docs; no shim)
</success_criteria>

<output>
Create `.planning/phases/09-backend-structure-refactor/09-02-SUMMARY.md` when done
</output>
