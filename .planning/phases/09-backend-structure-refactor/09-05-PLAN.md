---
phase: 09-backend-structure-refactor
plan: 05
type: execute
wave: 5
depends_on:
  - "09-04"
files_modified:
  - src/backend/server/__init__.py
  - src/backend/server/app.py
  - src/backend/server/auth.py
  - src/backend/server/observability.py
  - src/backend/server/schemas.py
  - README.md
autonomous: true
requirements:
  - SC-01
must_haves:
  truths:
    - "HTTP concerns live under server/ (app, auth, schemas, observability)"
    - "server imports agents one-way; agents never imports server"
    - "README documents uvicorn server.app:app from src/backend"
    - "Stream auth logic retains compare_digest and fail-closed behavior"
  artifacts:
    - path: "src/backend/server/app.py"
      provides: "FastAPI app + /stream lifespan"
      exports: ["app"]
    - path: "src/backend/server/auth.py"
      provides: "verify_stream_api_key"
      contains: "secrets.compare_digest"
    - path: "src/backend/server/schemas.py"
      provides: "ExtractData, UserData"
      contains: "from agents.models import TaroCard"
    - path: "README.md"
      provides: "Entrypoint docs"
      contains: "uvicorn server.app:app"
  key_links:
    - from: "src/backend/server/app.py"
      to: "agents.setup_workflow"
      via: "from agents import setup_workflow"
      pattern: "from agents import setup_workflow"
    - from: "src/backend/server/app.py"
      to: "src/backend/server/auth.py"
      via: "Depends(verify_stream_api_key)"
      pattern: "verify_stream_api_key"
    - from: "src/backend/server/schemas.py"
      to: "src/backend/agents/models.py"
      via: "TaroCard import"
      pattern: "from agents\\.models import TaroCard"
---

<objective>
Create the `server/` HTTP package wired to `agents.setup_workflow`, place ExtractData/UserData in server.schemas importing TaroCard from agents.models, and update README to `uvicorn server.app:app` — SC-01 and locked D-01, D-02, D-03.

Purpose: Explicit ASGI entrypoint with auth unchanged on `/stream`.
Output: `src/backend/server/**` + README updates; legacy root modules remain until Plan 06.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-structure-refactor/09-CONTEXT.md
@.planning/phases/09-backend-structure-refactor/09-RESEARCH.md
@.planning/phases/09-backend-structure-refactor/09-VALIDATION.md
@.planning/phases/09-backend-structure-refactor/09-04-SUMMARY.md

@src/backend/app.py
@src/backend/auth.py
@src/backend/observability.py
@src/backend/schemas.py
@README.md
@pyproject.toml
</context>

<interface>
## Contracts consumed from Plan 04

- `from agents import setup_workflow`
- `from agents.models import TaroCard`

## Contracts this plan creates

- `server.app:app`
- `server.auth.verify_stream_api_key`
- `server.observability.build_langfuse_callbacks`
- `server.schemas.ExtractData`, `server.schemas.UserData`
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Create server package with one-way agents imports</name>
  <files>src/backend/server/__init__.py, src/backend/server/app.py, src/backend/server/auth.py, src/backend/server/observability.py, src/backend/server/schemas.py</files>
  <read_first>
    src/backend/app.py
    src/backend/auth.py
    src/backend/observability.py
    src/backend/schemas.py
    src/backend/agents/__init__.py
  </read_first>
  <action>
    Create `src/backend/server/` per D-01 and D-02 (server = HTTP only).

    1. Copy auth.py → server/auth.py with zero logic changes: secrets.compare_digest, fail-closed 500 when STREAM_API_KEY unset, X-API-Key + Bearer, 401 on missing/invalid. Do not weaken stream auth (T-09-01 / ASVS L1).
    2. Copy observability.py → server/observability.py unchanged in behavior.
    3. Create server/schemas.py with ExtractData and UserData; import TaroCard via `from agents.models import TaroCard` (no duplicate TaroCard; no ExtractData in agents).
    4. Create server/app.py from old app.py with imports: server.auth, server.observability, server.schemas, `from agents import setup_workflow`. Keep object named `app` and `/stream` behavior.
    5. Minimal server/__init__.py.

    Hard rules: agents must not import server. No root app.py shim (D-03/D-04 — leave old roots for Plan 06 delete). Keep pyproject.toml pythonpath as ["src/backend"].
  </action>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && PYTHONPATH=src/backend python -c "from server.app import app; from server.auth import verify_stream_api_key; from server.schemas import ExtractData, UserData; from agents.models import TaroCard; assert hasattr(app, 'routes'); print('ok')" && ! rg -n "from server|import server" src/backend/agents && rg -n "compare_digest" src/backend/server/auth.py</automated>
  </verify>
  <done>server.app imports; auth retains compare_digest; agents has zero imports of server.</done>
</task>

<task type="auto">
  <name>Task 2: Update README entrypoint and layout docs</name>
  <files>README.md</files>
  <read_first>
    README.md
  </read_first>
  <action>
    Update README per D-03: replace `uvicorn app:app` with `uvicorn server.app:app` (still cd src/backend then uv run uvicorn server.app:app --reload --port 8000). Update layout blurb to server/ + agents/. No root compatibility shim instruction. Leave pytest pythonpath note as src/backend.
  </action>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && rg -n "uvicorn server.app:app" README.md && ! rg -n "uvicorn app:app" README.md</automated>
  </verify>
  <done>README documents server.app:app only; no legacy uvicorn app:app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server.app /stream | Untrusted HTTP; API key required |
| server → agents | Trusted internal import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | server.auth.verify_stream_api_key | mitigate | Relocate auth without changing compare_digest / fail-closed / header+Bearer; block if any weakening |
| T-09-02 | Elevation of Privilege | Compatibility shims | mitigate | No root app.py re-export; dual tree only until Plan 06 delete (D-03/D-04) |
| T-09-03 | Information Disclosure | server.observability | accept | Langfuse keys remain env-only |
| T-09-04 | Tampering | server.schemas | mitigate | Keep Pydantic validation; TaroCard from agents.models |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
- from server.app import app works
- agents does not import server
- README has uvicorn server.app:app only
- compare_digest present in server.auth
</verification>

<success_criteria>
- SC-01 clear server package
- D-01/D-02/D-03 server half complete
</success_criteria>

<output>
Create `.planning/phases/09-backend-structure-refactor/09-05-SUMMARY.md` when done
</output>
