---
phase: 11-agent-harness-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/backend/agents/factories.py
  - src/backend/agents/state.py
  - src/backend/agents/cards/factory.py
  - src/backend/agents/cards/prompt.py
  - src/backend/agents/cards/node.py
  - src/backend/server/schemas.py
  - src/frontend/schema.py
  - src/frontend/pages/app.py
  - src/frontend/templates.py
  - tests/conftest.py
  - tests/test_spread_name_schema.py
  - README.md
autonomous: true
requirements:
  - SC-11.1
must_haves:
  truths:
    - "unlock_card_agent, UnlockCard, create_card_unlock_agent, and unlock prompt are gone (D-01)"
    - "No unlock_name field remains on AgentState, ExtractData, or frontend session/message payloads (D-02)"
    - "UI/API reading title uses spread_name from MCP mapping via img_node (D-03)"
    - "make_mock_agents and create_agents match Agents dataclass without unlock field"
  artifacts:
    - path: "src/backend/agents/state.py"
      provides: "Agents without unlock_card_agent; AgentState.spread_name"
      contains: "spread_name"
    - path: "src/backend/agents/factories.py"
      provides: "create_agents without unlock"
    - path: "src/backend/agents/cards/node.py"
      provides: "img_node emits spread_name"
      contains: "spread_name"
    - path: "src/backend/server/schemas.py"
      provides: "ExtractData.spread_name"
    - path: "src/frontend/pages/app.py"
      provides: "session/messages use spread_name with create_html_taro"
  key_links:
    - from: "src/backend/agents/cards/mapping.py"
      to: "src/backend/agents/cards/node.py"
      via: "extract_cards_from_messages -> spread_name"
      pattern: "spread_name"
    - from: "src/backend/server/schemas.py"
      to: "src/frontend/schema.py"
      via: "ExtractData.spread_name field parity"
      pattern: "spread_name"
    - from: "src/frontend/pages/app.py"
      to: "src/frontend/templates.py"
      via: "create_html_taro(cards, spread_name)"
      pattern: "create_html_taro"
---

<objective>
Hard-delete the dead unlock LLM surface and rename unlock_name → spread_name end-to-end so the UI title comes from MCP spread/card mapping (D-01, D-02, D-03).

Purpose: SC-11.1 — unlock agent is unused after Phase 4 MCP mapping; keeping the field breaks mocks and confuses the title path.
Output: Deleted cards factory/prompt; updated Agents/state/schemas/frontend; Wave 0 schema/node tests; green pytest.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-harness-hardening/11-CONTEXT.md
@.planning/phases/11-agent-harness-hardening/11-RESEARCH.md
@.planning/phases/11-agent-harness-hardening/11-VALIDATION.md
@.planning/phases/11-agent-harness-hardening/11-01-SUMMARY.md

@src/backend/agents/factories.py
@src/backend/agents/state.py
@src/backend/agents/cards/node.py
@src/backend/agents/cards/mapping.py
@src/backend/server/schemas.py
@src/frontend/schema.py
@src/frontend/pages/app.py
@tests/conftest.py
</context>

<interface>
## Contracts after this plan

- `Agents` dataclass fields: taro_agent, taro_tool, astro_agent, astro_tool, router_agent, summarize_agent only (no unlock_card_agent)
- `AgentState.spread_name: str` replaces `unlock_name`
- `img_node` return dict key `spread_name` from `extract_cards_from_messages` (mapping already returns spread_name locally)
- `server.schemas.ExtractData.spread_name: Optional[str]`
- `frontend.schema` + session/message keys use `spread_name`
- Deleted: `agents.cards.factory`, `agents.cards.prompt`, `UnlockCard` model
- Kept: `agents.cards.mapping`, `img_node` path
</interface>

## Artifacts this phase produces

| Symbol / path | Introduced in |
|---------------|---------------|
| Landed Langfuse v4 | 11-01 |
| Unlock deleted; spread_name E2E | 11-02 |
| Trust wrap + cap logging | 11-03 |
| DeepEval evals | 11-04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete unlock agent surface and rename state/API to spread_name</name>
  <files>src/backend/agents/factories.py, src/backend/agents/state.py, src/backend/agents/cards/factory.py, src/backend/agents/cards/prompt.py, src/backend/agents/cards/node.py, src/backend/server/schemas.py, tests/conftest.py, README.md</files>
  <read_first>
    src/backend/agents/state.py
    src/backend/agents/factories.py
    src/backend/agents/cards/factory.py
    src/backend/agents/cards/prompt.py
    src/backend/agents/cards/node.py
    src/backend/agents/cards/mapping.py
    src/backend/server/schemas.py
    tests/conftest.py
    README.md
    .planning/phases/11-agent-harness-hardening/11-RESEARCH.md
  </read_first>
  <behavior>
    - Importing agents.cards.factory or UnlockCard fails after delete
    - create_agents / make_mock_agents construct Agents without unlock_card_agent
    - img_node returns spread_name key (not unlock_name)
    - ExtractData model field is spread_name
    - rg unlock_card / UnlockCard / unlock_name under src/backend finds no production hits (tests may assert absence)
  </behavior>
  <action>
    Per D-01: hard-delete `src/backend/agents/cards/factory.py` and `src/backend/agents/cards/prompt.py`; remove `UnlockCard` from `state.py`; drop `unlock_card_agent` from `Agents` and from `create_agents` in `factories.py`. Per D-02/D-03 (Claude discretion rename): rename `AgentState.unlock_name` → `spread_name`; update `cards/node.py` to unpack mapping as `taro_cards, spread_name` and emit `spread_name`; rename `ExtractData.unlock_name` → `spread_name` in `server/schemas.py`. Update `tests/conftest.py` `make_mock_agents` in the same change to avoid TypeError. Remove README Unlock-card model row. Do not edit `cards/mapping.py` behavior (already returns spread_name). Do not remove astro (deferred). Prefer clean cut — no long dual-field unlock_name/spread_name compat layer. Grep `src/` and tests for leftover unlock symbols and fix. No default parameter values on new signatures.
  </action>
  <acceptance_criteria>
    - cards/factory.py and cards/prompt.py deleted
    - state.py has no UnlockCard and no unlock_card_agent / unlock_name
    - factories.py and conftest make_mock_agents align with Agents
    - img_node and ExtractData use spread_name
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && (rg -n "unlock_card_agent|UnlockCard|unlock_name|create_card_unlock_agent|unlock_card_prompt" src/backend || true) | tee /tmp/unlock-backend.txt; test ! -s /tmp/unlock-backend.txt; uv run pytest -q tests/test_router.py tests/test_taro_cards.py -x</automated>
  </verify>
  <done>D-01/D-02 backend surface removed; title field is spread_name from MCP mapping path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend spread_name migration + Wave 0 schema tests</name>
  <files>src/frontend/schema.py, src/frontend/pages/app.py, src/frontend/templates.py, tests/test_spread_name_schema.py</files>
  <read_first>
    src/frontend/schema.py
    src/frontend/pages/app.py
    src/frontend/templates.py
    src/backend/server/schemas.py
    src/backend/agents/cards/node.py
    .planning/phases/11-agent-harness-hardening/11-VALIDATION.md
  </read_first>
  <behavior>
    - Frontend ExtractData (or equivalent) field is spread_name
    - session_state and message dicts store spread_name; create_html_taro receives that title
    - No unlock_name references remain under src/frontend
    - Wave 0 test asserts ExtractData / img_node contract uses spread_name
  </behavior>
  <action>
    Per D-02/D-03: rename frontend schema field and all `pages/app.py` session/message keys from `unlock_name` to `spread_name`; update `create_html_taro` call sites. Optionally rename templates param `name` → `spread_name` for clarity (behavior unchanged — Claude discretion). Add `tests/test_spread_name_schema.py` (Wave 0) asserting server ExtractData accepts/exports `spread_name` and img_node return shape includes `spread_name` (mock messages / existing SAMPLE_MCP_READING patterns from test_taro_cards). Do not dual-write unlock_name. Keep UI visual check for later UAT (VALIDATION manual-only).
  </action>
  <acceptance_criteria>
    - rg unlock_name under src/frontend returns no matches
    - tests/test_spread_name_schema.py passes
    - uv run pytest -q green
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && (rg -n "unlock_name|unlock_card" src/frontend || true) | tee /tmp/unlock-fe.txt; test ! -s /tmp/unlock-fe.txt; uv run pytest -q tests/test_spread_name_schema.py tests/test_taro_cards.py tests/test_router.py -x && uv run pytest -q</automated>
  </verify>
  <done>SC-11.1 complete: unlock gone; MCP-derived spread_name flows API → UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool text → mapping → spread_name | Untrusted tool output becomes UI title string (display only) |
| API ExtractData → Streamlit | Client trusts field rename; mismatched keys blank the layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | cards/mapping → spread_name | accept | Title is display metadata from MCP; not executed as code; same trust as existing card HTML |
| T-11-04 | Elevation of Privilege | deleted unlock LLM | mitigate | Removing unlock agent reduces attack surface (no extra structured LLM for title) |
| T-11-SC | Tampering | package installs | accept | No new dependencies in this plan |
</threat_model>

<verification>
- Backend/frontend grep gates: no unlock_name / unlock_card leftovers under src/
- uv run pytest -q green
</verification>

<success_criteria>
- D-01 hard delete complete
- D-02/D-03 spread_name end-to-end
- SC-11.1 observable via missing unlock symbols + mapping-driven title field
</success_criteria>

<output>
Create `.planning/phases/11-agent-harness-hardening/11-02-SUMMARY.md` when done
</output>
