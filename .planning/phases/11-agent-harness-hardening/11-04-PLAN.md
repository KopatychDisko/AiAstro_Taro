---
phase: 11-agent-harness-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - "11-02"
  - "11-03"
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/evals/metrics.py
  - tests/evals/goldens_router.json
  - tests/evals/goldens_taro.json
  - tests/evals/test_router_eval.py
  - tests/evals/test_taro_eval.py
  - README.md
autonomous: false
requirements:
  - SC-11.5
  - SC-11.6
user_setup: []
must_haves:
  truths:
    - "deepeval is a dev dependency installed via uv after human legitimacy verify (D-12)"
    - "Offline eval suite covers router + taro only, not astro (D-13)"
    - "Default uv run pytest -q excludes eval mark and stays green/fast (SC-11.6)"
    - "No Confident AI login required to run the suite (D-12)"
    - "Goldens are hand-curated small JSON sets (not deepeval generate)"
  artifacts:
    - path: "tests/evals/metrics.py"
      provides: "DeepEval metric instances for router/taro"
    - path: "tests/evals/test_router_eval.py"
      provides: "marked eval tests for router"
    - path: "tests/evals/test_taro_eval.py"
      provides: "marked eval tests for taro"
    - path: "tests/evals/goldens_router.json"
      provides: "hand-curated router goldens"
    - path: "tests/evals/goldens_taro.json"
      provides: "hand-curated taro goldens"
    - path: "pyproject.toml"
      provides: "eval marker + addopts -m 'not eval' + deepeval dev dep"
  key_links:
    - from: "pyproject.toml"
      to: "tests/evals/"
      via: "markers.eval + addopts not eval"
      pattern: "not eval"
    - from: "tests/evals/test_router_eval.py"
      to: "tests/evals/metrics.py"
      via: "assert_test + ROUTER metrics"
      pattern: "assert_test|deepeval"
---

<objective>
Add an offline DeepEval+pytest eval harness for router + taro behind an eval marker so default pytest stays green and fast (D-12, D-13, SC-11.5, SC-11.6).

Purpose: Lightweight regression beyond unit mocks without Confident AI or live cloud gates.
Output: `uv add --dev deepeval` after legitimacy checkpoint; `tests/evals/` with hand-curated goldens; pytest marker/`addopts`; README run instructions.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-harness-hardening/11-CONTEXT.md
@.planning/phases/11-agent-harness-hardening/11-RESEARCH.md
@.planning/phases/11-agent-harness-hardening/11-VALIDATION.md
@.planning/phases/11-agent-harness-hardening/11-02-SUMMARY.md
@.planning/phases/11-agent-harness-hardening/11-03-SUMMARY.md
@.agents/skills/deepeval/SKILL.md
@.agents/skills/deepeval/references/pytest-e2e-evals.md
@.agents/skills/deepeval/references/artifact-contracts.md

@tests/test_router.py
@pyproject.toml
</context>

<interface>
## Contracts this plan creates

- pytest marker `eval` registered in `[tool.pytest.ini_options] markers`
- `addopts` includes `-m "not eval"` so default collection skips evals (SC-11.6)
- `tests/evals/metrics.py` — shared metric instances (Claude discretion: GEval or deterministic custom; keep offline)
- Hand-curated `goldens_router.json` / `goldens_taro.json` (small sets; not `deepeval generate`)
- `@pytest.mark.eval` tests calling `deepeval.assert_test` with mocked/local router+taro runners
- Documented command: `uv run deepeval test run tests/evals` (or equivalent marker-select run)
</interface>

## Artifacts this phase produces

| Symbol / path | Introduced in |
|---------------|---------------|
| Langfuse / unlock / trust+cap | 11-01..11-03 |
| `tests/evals/` + deepeval dev dep | 11-04 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify deepeval package legitimacy</name>
  <files>pyproject.toml</files>
  <what-built>
    RESEARCH Package Legitimacy Audit flagged `deepeval` as [SUS] (seam: incomplete download metadata) while confirming official docs/repo at github.com/confident-ai/deepeval and deepeval.com. D-12 locks the package name; human must confirm PyPI ↔ GitHub match before `uv add --dev`.
  </what-built>
  <action>
    Pause for blocking human legitimacy check of `deepeval` on PyPI vs GitHub before any install (T-11-SC / RESEARCH [SUS]). Do not run `uv add` until resume-signal approved. D-12 still locks the package name after approval.
  </action>
  <how-to-verify>
    1. Open https://pypi.org/project/deepeval/ and confirm project links to https://github.com/confident-ai/deepeval
    2. Open https://github.com/confident-ai/deepeval and confirm it is the Confident AI / DeepEval project
    3. Confirm latest/relevant version is plausible (RESEARCH noted 4.1.1 at research time — re-check current)
    4. Reply approved if legitimate, or describe issues if not
  </how-to-verify>
  <verify>
    <automated>MISSING — human gate only; no install until approved</automated>
  </verify>
  <done>Human approved deepeval as legitimate PyPI↔GitHub match; executor may proceed to uv add --dev.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install deepeval and scaffold marked offline evals</name>
  <files>pyproject.toml, uv.lock, tests/evals/metrics.py, tests/evals/goldens_router.json, tests/evals/goldens_taro.json, tests/evals/test_router_eval.py, tests/evals/test_taro_eval.py, README.md</files>
  <read_first>
    pyproject.toml
    tests/test_router.py
    .agents/skills/deepeval/references/pytest-e2e-evals.md
    .agents/skills/deepeval/references/artifact-contracts.md
    .agents/skills/deepeval/templates/test_single_turn_no_tracing.py
    .planning/phases/11-agent-harness-hardening/11-RESEARCH.md
    .planning/phases/11-agent-harness-hardening/11-VALIDATION.md
  </read_first>
  <behavior>
    - uv add --dev deepeval succeeds after approval
    - pytest markers include eval; addopts excludes eval from default runs
    - uv run pytest -q collects zero eval tests and remains green (SC-11.6)
    - tests/evals covers router + taro only (D-13); no astro eval file
    - Goldens are committed hand-curated JSON (5–10 cases total scale), not generated via deepeval generate
    - Suite does not require deepeval login / Confident AI (D-12)
  </behavior>
  <action>
    After Task 1 approval only: run `uv add --dev deepeval` (uv exclusively). Register pytest marker `eval` and set `addopts` so default runs use `-m "not eval"` without breaking existing asyncio/pythonpath settings. Create `tests/evals/` with `metrics.py`, hand-curated `goldens_router.json` and `goldens_taro.json` (honor user/research: hand-curate — do not run `deepeval generate`), and `@pytest.mark.eval` tests using DeepEval `assert_test` + `LLMTestCase` (no-tracing offline shape). Router evals should drive mocked router next_node intent; taro evals use one MCP fixture / mapping-adjacent path — Claude discretion on exact GEval criteria. Do not add Confident AI login steps, tracing skill wiring, or astro coverage (D-13). Document how to run evals in README (`uv run deepeval test run tests/evals` and/or `uv run pytest -m eval`). No new required app env keys; judge may use existing OPENAI_API_KEY when evals are explicitly run. No default parameter values on new Python helpers.
  </action>
  <acceptance_criteria>
    - deepeval listed under dependency-groups.dev / uv.lock
    - pyproject has markers.eval and addopts excluding eval
    - tests/evals has router + taro only
    - uv run pytest -q green and does not execute eval tests
    - README documents offline eval run command
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q && uv run pytest -q -m eval --collect-only 2>&1 | tee /tmp/eval-collect.txt; rg -n "test_router_eval|test_taro_eval|eval" /tmp/eval-collect.txt</automated>
  </verify>
  <done>SC-11.5 harness exists offline; SC-11.6 default suite excludes evals and stays green; D-12/D-13 met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval suite → OpenAI judge (optional when run) | Explicit offline eval run may call LLM judge with golden inputs |
| eval suite → Confident AI | Must not exfiltrate; no login this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information Disclosure | tests/evals + deepeval | mitigate | Offline only; no Confident AI login (D-12); default pytest excludes eval mark so CI/dev quick path never phones home |
| T-11-08 | Denial of Service | LLM-judge evals | mitigate | Marker + addopts keep default suite fast; evals opt-in only (SC-11.6) |
| T-11-SC | Tampering | uv add deepeval | mitigate | Blocking human-verify against PyPI↔GitHub before install ([SUS] disposition from RESEARCH); reject [SLOP] |
</threat_model>

<verification>
- uv run pytest -q green (evals excluded)
- Eval collection shows router/taro eval tests under -m eval
- README documents offline run without Confident AI
</verification>

<success_criteria>
- D-12 DeepEval offline without Confident AI
- D-13 router + taro coverage only
- SC-11.5 and SC-11.6 satisfied
</success_criteria>

<output>
Create `.planning/phases/11-agent-harness-hardening/11-04-SUMMARY.md` when done
</output>
