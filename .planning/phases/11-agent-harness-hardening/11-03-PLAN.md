---
phase: 11-agent-harness-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "11-01"
  - "11-02"
files_modified:
  - src/backend/agents/routing.py
  - src/backend/agents/context_trust.py
  - src/backend/agents/workflow.py
  - tests/test_routing.py
  - tests/test_context_trust.py
  - README.md
autonomous: true
requirements:
  - SC-11.2
  - SC-11.3
must_haves:
  truths:
    - "Hitting MAX_TOOL_ITERATIONS logs a stable warning with structured extras then returns __end__ with no user-facing limit copy (D-04)"
    - "MAX_TOOL_ITERATIONS remains the named constant 3 with code comment + README note; not env-configurable (D-05, D-06)"
    - "Zep/user memory is always wrapped via wrap_untrusted_user_memory in take_context only (D-07, D-08, D-09)"
    - "Name-only Zep failure fallback is also wrapped"
  artifacts:
    - path: "src/backend/agents/routing.py"
      provides: "capped_tools_condition with structured cap log"
      contains: "tool_iteration_cap_reached"
    - path: "src/backend/agents/context_trust.py"
      provides: "pure wrap_untrusted_user_memory"
      exports:
        - "wrap_untrusted_user_memory"
        - "UNTRUSTED_USER_MEMORY_PREFIX"
    - path: "src/backend/agents/workflow.py"
      provides: "take_context always wraps context"
    - path: "tests/test_context_trust.py"
      provides: "Wave 0 trust wrapper unit tests"
    - path: "tests/test_routing.py"
      provides: "caplog assertion on cap"
  key_links:
    - from: "src/backend/agents/workflow.py"
      to: "src/backend/agents/context_trust.py"
      via: "wrap_untrusted_user_memory in take_context"
      pattern: "wrap_untrusted_user_memory"
    - from: "src/backend/agents/routing.py"
      to: "logger.warning"
      via: "tool_iteration_cap_reached + extra tool_iterations"
      pattern: "tool_iteration_cap_reached"
---

<objective>
Make the tool-loop budget observable (structured log on cap) and trust-label all Zep/user memory at take_context assembly (D-04 through D-09).

Purpose: SC-11.2 / SC-11.3 — cap already returns __end__; add logging + docs only. Trust labeling is a single pure wrapper at assembly, not per-agent prompts.
Output: `context_trust.py`, updated `routing.py` / `take_context`, Wave 0 tests, README budget note.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-harness-hardening/11-CONTEXT.md
@.planning/phases/11-agent-harness-hardening/11-RESEARCH.md
@.planning/phases/11-agent-harness-hardening/11-VALIDATION.md
@.planning/phases/11-agent-harness-hardening/11-01-SUMMARY.md
@.planning/phases/11-agent-harness-hardening/11-02-SUMMARY.md

@src/backend/agents/routing.py
@src/backend/agents/workflow.py
@tests/test_routing.py
</context>

<interface>
## Contracts this plan creates

- `agents.context_trust.UNTRUSTED_USER_MEMORY_PREFIX: str` — English prefix including UNTRUSTED_USER_MEMORY and treat-as-data line (Claude discretion exact wording; RESEARCH Pattern 2 as default)
- `agents.context_trust.wrap_untrusted_user_memory(body: str) -> str` — pure; returns prefix + body; no I/O; no mutation of inputs
- `agents.routing.MAX_TOOL_ITERATIONS = 3` — named constant; comment that it is not env-configurable (D-05)
- `capped_tools_condition` on cap: `logger.warning("tool_iteration_cap_reached", extra={...})` then return `"__end__"` unchanged (D-04)
</interface>

## Artifacts this phase produces

| Symbol / path | Introduced in |
|---------------|---------------|
| Langfuse v4 landed | 11-01 |
| spread_name E2E | 11-02 |
| `context_trust.py` + cap logging | 11-03 |
| DeepEval suite | 11-04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap logging on MAX_TOOL_ITERATIONS</name>
  <files>src/backend/agents/routing.py, tests/test_routing.py, README.md</files>
  <read_first>
    src/backend/agents/routing.py
    tests/test_routing.py
    src/backend/server/app.py
    .planning/phases/11-agent-harness-hardening/11-RESEARCH.md
    README.md
  </read_first>
  <behavior>
    - When tool_iterations greater than or equal to MAX_TOOL_ITERATIONS: return __end__ and emit warning tool_iteration_cap_reached
    - Log extras include tool_iterations and max_tool_iterations; message string has no interpolated dynamics
    - Below cap: still delegates to tools_condition (existing behavior)
    - No user-facing limit string added anywhere in graph nodes
  </behavior>
  <action>
    Per D-04/D-05/D-06: keep `MAX_TOOL_ITERATIONS = 3` as a named constant; add a short code comment that it is the tool-loop budget and not env-configurable this phase. In `capped_tools_condition`, before returning `__end__`, log with `logging.getLogger(__name__)` and stable message `tool_iteration_cap_reached` plus `extra` keys `tool_iterations` and `max_tool_iterations` (match server/app.py structured logging style; do not interpolate dynamics into the message). Do not change the return value or edge map. Do not add token/cost/step budgets (D-06). Document the constant briefly in README. Extend `tests/test_routing.py` with caplog assertion. No default parameter values on new functions.
  </action>
  <acceptance_criteria>
    - routing.py logs tool_iteration_cap_reached on cap
    - MAX_TOOL_ITERATIONS remains 3 with documenting comment
    - README mentions the tool-loop budget of 3
    - test_routing.py asserts log via caplog
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_routing.py -x</automated>
  </verify>
  <done>SC-11.2: budget explicit, documented, logged on enforce, silent to user.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure trust wrap in take_context</name>
  <files>src/backend/agents/context_trust.py, src/backend/agents/workflow.py, tests/test_context_trust.py</files>
  <read_first>
    src/backend/agents/workflow.py
    .planning/phases/11-agent-harness-hardening/11-RESEARCH.md
    .planning/phases/11-agent-harness-hardening/11-VALIDATION.md
  </read_first>
  <behavior>
    - wrap_untrusted_user_memory(body) returns a string starting with UNTRUSTED_USER_MEMORY and including treat-as-data guidance, then body
    - Empty body still gets the wrapper prefix
    - take_context success path and exception name-only fallback both call the wrapper once before returning context
    - No duplicate trust labels added to router/taro/astro system prompts
  </behavior>
  <action>
    Per D-07/D-08/D-09: create pure `wrap_untrusted_user_memory(body: str) -> str` in new `src/backend/agents/context_trust.py` (Claude discretion: RESEARCH Pattern 2 prefix wording). Wire `take_context` in `workflow.py` so both the Zep success body and the exception name-only body are passed through the wrapper before `return {'context': ...}`. Do not mutate input strings in place; do not put trust labels into per-agent prompts. Add Wave 0 `tests/test_context_trust.py` covering wrapper purity and (optionally) that take_context output contains the prefix when Zep is mocked success/failure. No default parameter values.
  </action>
  <acceptance_criteria>
    - context_trust.py exports wrap_untrusted_user_memory
    - workflow take_context uses wrapper on both paths
    - tests/test_context_trust.py passes
    - rg UNTRUSTED_USER_MEMORY under agents/*/prompt.py is empty (assembly-only)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_context_trust.py tests/test_routing.py -x && (rg -n "UNTRUSTED_USER_MEMORY" src/backend/agents --glob '*prompt*' || true) | tee /tmp/trust-prompts.txt; test ! -s /tmp/trust-prompts.txt; uv run pytest -q</automated>
  </verify>
  <done>SC-11.3: untrusted memory always labeled at assembly; D-04–D-09 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zep cloud → take_context | Untrusted user memory / injection surface crosses into graph context |
| tool loop → cost/DoS | Agent tool iterations bounded by MAX_TOOL_ITERATIONS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering / Elevation | take_context + context_trust.py | mitigate | Always wrap with UNTRUSTED_USER_MEMORY + treat-as-data (D-07–D-09); assembly-only to avoid prompt drift |
| T-11-06 | Denial of Service | capped_tools_condition | mitigate | Cap at 3 iterations; structured log on hit; silent stop without new user-facing copy (D-04–D-06) |
| T-11-SC | Tampering | package installs | accept | No new dependencies in this plan |
</threat_model>

<verification>
- uv run pytest -q tests/test_routing.py tests/test_context_trust.py
- Full uv run pytest -q green
- Prompt packages do not duplicate UNTRUSTED_USER_MEMORY
</verification>

<success_criteria>
- SC-11.2 and SC-11.3 true
- D-04..D-09 implemented without deferred token budgets or env-configurable cap
</success_criteria>

<output>
Create `.planning/phases/11-agent-harness-hardening/11-03-SUMMARY.md` when done
</output>
