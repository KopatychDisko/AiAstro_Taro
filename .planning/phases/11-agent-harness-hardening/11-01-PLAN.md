---
phase: 11-agent-harness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/server/observability.py
  - src/backend/server/app.py
  - src/backend/agents/router/node.py
  - src/backend/agents/taro/node.py
  - src/backend/agents/astro/node.py
  - src/backend/agents/workflow.py
  - tests/test_observability.py
  - tests/test_langfuse_config_prop.py
  - README.md
autonomous: true
requirements:
  - SC-11.4
  - SC-11.6
must_haves:
  truths:
    - "Langfuse CallbackHandler is constructed with no session ctor args when keys present (D-10)"
    - "RunnableConfig carries langfuse_session_id / langfuse_user_id / langfuse_tags metadata into stream"
    - "router, taro, astro, and summarize ainvoke receive config when callbacks are enabled"
    - "flush_langfuse runs after stream when callbacks were used; empty callbacks when keys absent (D-11)"
    - "pytest -q stays green with no new required env keys (SC-11.6)"
  artifacts:
    - path: "src/backend/server/observability.py"
      provides: "v4 build_langfuse_callbacks, langfuse_run_metadata, flush_langfuse"
      exports:
        - "build_langfuse_callbacks"
        - "langfuse_run_metadata"
        - "flush_langfuse"
    - path: "src/backend/server/app.py"
      provides: "stream config metadata + finally flush"
    - path: "tests/test_observability.py"
      provides: "mocked CallbackHandler unit tests"
    - path: "tests/test_langfuse_config_prop.py"
      provides: "Wave 0 integration: ainvoke receives config"
  key_links:
    - from: "src/backend/server/app.py"
      to: "src/backend/server/observability.py"
      via: "build_langfuse_callbacks + langfuse_run_metadata + flush_langfuse"
      pattern: "langfuse_run_metadata|flush_langfuse"
    - from: "src/backend/agents/router/node.py"
      to: "RunnableConfig"
      via: "ainvoke(..., config=config)"
      pattern: "ainvoke\\([^)]*config=config"
    - from: "src/backend/agents/workflow.py"
      to: "summarize_agent.ainvoke"
      via: "config=config"
      pattern: "summarize_agent\\.ainvoke"
---

<objective>
Land uncommitted Langfuse SDK v4 wiring and prove optional CallbackHandler + metadata propagation with mocked unit/integration tests (D-10, D-11).

Purpose: SC-11.4 requires verified v4 tracing path before other harness edits share the same tree; working-tree diff already exists — commit and complete Wave 0 config-prop tests, do not reinvent.
Output: Committed observability helpers, node/workflow config propagation, `tests/test_langfuse_config_prop.py`, green `pytest -q`.
</objective>

<execution_context>
@/home/egor/projects/AiTaro/.cursor/gsd-core/workflows/execute-plan.md
@/home/egor/projects/AiTaro/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-harness-hardening/11-CONTEXT.md
@.planning/phases/11-agent-harness-hardening/11-RESEARCH.md
@.planning/phases/11-agent-harness-hardening/11-VALIDATION.md

@src/backend/server/observability.py
@src/backend/server/app.py
@src/backend/agents/router/node.py
@src/backend/agents/taro/node.py
@src/backend/agents/astro/node.py
@src/backend/agents/workflow.py
@tests/test_observability.py
</context>

<interface>
## Contracts this plan lands (already drafted in working tree)

- `server.observability.build_langfuse_callbacks(session_id: str, user_name: str) -> list[object]` — empty list when keys absent; `[CallbackHandler()]` when present (no session kwargs on ctor)
- `server.observability.langfuse_run_metadata(session_id: str, user_name: str) -> dict[str, object]` — keys `langfuse_session_id`, `langfuse_user_id`, `langfuse_tags`, plus `user_name`
- `server.observability.flush_langfuse() -> None` — no-op without keys; else `get_client().flush()`
- Router/taro/astro nodes and summarize in `workflow.add_memory`: `ainvoke(..., config=config)` so callbacks/metadata propagate
- Astro config propagation kept while astro exists (Claude discretion / RESEARCH)
</interface>

## Artifacts this phase produces

| Symbol / path | Introduced in |
|---------------|---------------|
| Landed Langfuse v4 helpers + stream flush | 11-01 |
| `tests/test_langfuse_config_prop.py` | 11-01 |
| Unlock delete + `spread_name` rename | 11-02 |
| `agents/context_trust.py` + cap logging | 11-03 |
| `tests/evals/` DeepEval suite | 11-04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land Langfuse v4 wiring from working tree</name>
  <files>src/backend/server/observability.py, src/backend/server/app.py, src/backend/agents/router/node.py, src/backend/agents/taro/node.py, src/backend/agents/astro/node.py, src/backend/agents/workflow.py, tests/test_observability.py, README.md</files>
  <read_first>
    src/backend/server/observability.py
    src/backend/server/app.py
    src/backend/agents/router/node.py
    src/backend/agents/taro/node.py
    src/backend/agents/astro/node.py
    src/backend/agents/workflow.py
    tests/test_observability.py
    .planning/phases/11-agent-harness-hardening/11-RESEARCH.md
    git --no-pager diff HEAD -- src/backend/server/observability.py src/backend/server/app.py
  </read_first>
  <behavior>
    - Without LANGFUSE_PUBLIC_KEY/SECRET_KEY (or blank): build_langfuse_callbacks returns []
    - With keys: returns one CallbackHandler constructed with zero session/user kwargs
    - langfuse_run_metadata exposes langfuse_session_id, langfuse_user_id, langfuse_tags
    - flush_langfuse no-ops without keys; calls get_client().flush when keys present
    - Existing tests/test_observability.py pass with mocks
  </behavior>
  <action>
    Per D-10: land the existing uncommitted Langfuse v4 diff — do not rewrite a second wrapper API. Ensure `observability.py` uses `CallbackHandler()` from `langfuse.langchain`, metadata helpers, and `flush_langfuse` via `get_client().flush()`. Wire `app.py` stream to put callbacks + `langfuse_run_metadata` on RunnableConfig and call `flush_langfuse` in `finally` when callbacks were non-empty. Ensure router/taro/astro nodes and summarize `ainvoke` pass `config=config` (keep astro while it exists — Claude discretion). Update README Langfuse sentence if the WIP already does. Do not require Langfuse keys (D-11 / Phase 10 D-12). Do not add live cloud smoke. Optional: delete orphan `src/new_temp.py` if still present as deleted in the WIP. No new PyPI packages in this plan (langfuse already installed). No default parameter values on any new Python signatures.
  </action>
  <acceptance_criteria>
    - observability exports build_langfuse_callbacks, langfuse_run_metadata, flush_langfuse
    - app stream uses metadata keys and flush_langfuse
    - router/taro/astro/summarize ainvoke call sites include config=config
    - tests/test_observability.py green
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_observability.py -x</automated>
  </verify>
  <done>Uncommitted Langfuse v4 wiring is the committed source of truth; unit mocks green; keys remain optional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 config-propagation integration tests</name>
  <files>tests/test_langfuse_config_prop.py</files>
  <read_first>
    tests/test_observability.py
    tests/test_stream_endpoint.py
    src/backend/agents/router/node.py
    src/backend/agents/taro/node.py
    src/backend/agents/astro/node.py
    src/backend/agents/workflow.py
    .planning/phases/11-agent-harness-hardening/11-VALIDATION.md
  </read_first>
  <behavior>
    - With mocked agents and keys set: router/taro/astro node ainvoke receives a config mapping that includes callbacks and/or metadata keys used by Langfuse
    - Summarize path in add_memory (or equivalent unit under test) passes config into summarize_agent.ainvoke
    - Without keys: callbacks list empty and stream/helpers still succeed (reuse existing empty-callback patterns)
    - No live Langfuse HTTP (D-11)
  </behavior>
  <action>
    Create Wave 0 file `tests/test_langfuse_config_prop.py` per 11-VALIDATION.md / RESEARCH Validation Architecture for SC-11.4. Patch agent `ainvoke` (AsyncMock) and inspect `call_args` for `config`. Prefer thin unit tests over full MCP lifespan TestClient. Do not call Confident AI or Langfuse cloud. Keep suite fast so SC-11.6 holds.
  </action>
  <acceptance_criteria>
    - tests/test_langfuse_config_prop.py exists and passes
    - Full uv run pytest -q green (SC-11.6)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/egor/projects/AiTaro && uv run pytest -q tests/test_observability.py tests/test_langfuse_config_prop.py -x && uv run pytest -q</automated>
  </verify>
  <done>SC-11.4 verified by mocks; default suite green; no new required env keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → observability | Optional LANGFUSE_* keys gate tracing; absence must not break stream |
| app → Langfuse cloud | Outbound telemetry only when keys present; not a phase gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | server/observability.py, app.py | mitigate | Keys optional (D-11); empty callbacks when absent; never log secret key values; verification uses mocks only — no live cloud gate |
| T-11-02 | Tampering | CallbackHandler metadata | accept | Metadata is server-composed session/user tags; low risk if keys stolen — same as Phase 7 posture |
| T-11-SC | Tampering | npm/pip installs | accept | No new package install in this plan; langfuse already in lockfile |
</threat_model>

<verification>
- uv run pytest -q tests/test_observability.py tests/test_langfuse_config_prop.py
- uv run pytest -q full suite green (SC-11.6)
- No LANGFUSE_* added to required_env
</verification>

<success_criteria>
- D-10 wiring landed and committed as part of execution
- D-11 mock verification covers callbacks, metadata, flush, config prop
- SC-11.4 and SC-11.6 satisfied for observability slice
</success_criteria>

<output>
Create `.planning/phases/11-agent-harness-hardening/11-01-SUMMARY.md` when done
</output>
